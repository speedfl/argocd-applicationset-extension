apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: guestbook
spec:
  goTemplate: true
  generators:
  - list:
      elements:
      - name: guestbook-one
        url: https://kubernetes.default.svc
      - name: guestbook-two
        url: https://kubernetes.default.svc
      - name: guestbook-three
        url: https://kubernetes.default.svc
      - name: guestbook-four
        url: https://kubernetes.default.svc
      - name: guestbook-five
        url: https://kubernetes.default.svc
  template:
    metadata:
      name: '{{.name}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/argoproj/argocd-example-apps.git
        targetRevision: master
        path: guestbook
      destination:
        server: '{{.url}}'
        namespace: guestbook
